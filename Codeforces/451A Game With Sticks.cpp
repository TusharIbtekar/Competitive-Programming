#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n, m, i, cnt = 0, div;
    cin >> n >> m;
    if(min(n, m) % 2)
        cout << "Akshat";
    else
        cout << "Malvika";
}
