#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    cin >> s;
    int i, mark;
    for(i = s.size()-1; i >= 0; i--)
    {
        if(s[i] != 'a')
            mark = i;

    }
}
